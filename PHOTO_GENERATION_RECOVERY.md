# 图片生成等待与加载恢复

修改客户端图片生命周期、详情页刷新和连续语音录入反馈；不修改后端协议、文字生成请求、图片 marker 或上传格式。

- 编辑更新引入的新图片记录为生成图，显示制作中并轮询。
- 按完整的 owner scope + 图片 key 保存生成标记、首次等待时间和已显示标记；不存 Token 或指令正文。
- 退出重进、页面重建保留等待期限；无来源记录的图片使用“正在获取图片”，仍有最多 5 分钟恢复窗口。
- 已显示过的普通图片读取失败，自动重试窗口为 15 秒。生成图/未知图的失败请求间隔约 3 秒，最多 5 分钟。
- 超时后再进入仍尝试读取已晚到的结果；手动重试重置等待时间，仅重新加载，不再次提交生成任务。
- Android 下载并解码 Bitmap 后才结束等待；鸿蒙收到 Image 的完成回调后才结束等待，解码失败沿用原期限，退出后的旧回调被忽略。
- 每张图独立计时，图片等待不会阻止正文更新。后端没有任务状态时无法准确判断生成是否失败，使用“暂时无法显示”。

## 设备回归步骤

1. 生成公众号题图、解释图和自定义多张插画，检查缺图阶段等待、出图后真实比例显示。
2. 对已有图片应用卡通、水彩、素描、油画等指令，检查新 key 正常进入制作等待。
3. 生成过程中退出重进、切换版本或改写正文：文字正常更新；等待时间不反复重置。
4. 断网后恢复，分别测试已显示过的旧图和首次打开的未知图片；前者显示普通加载，后者显示中性获取提示。
5. 模拟已下载文件损坏，确认恢复重试直到显示成功或达到期限，不能无限等待。
6. 超时点击重试，检查没有新增文章编辑/绘图请求；生成超过期限后重进，仍能读取已完成图片。
7. 切换账号、快速离开页面，确认不复用其他 owner 的状态，旧请求不覆盖当前图片。

真机生成、网络故障与长时间等待仍需按上述步骤验证；构建和本地状态测试不能替代平台显示验证。

验证：Android Studio JBR 下 `./gradlew testDebugUnitTest assembleDebug` 通过（583 项 JVM 测试）。

分支验证：图片修复已迁移到 main，未合并 feature/wechat-subscription。main 上完整 JVM 测试与 Debug 构建通过，Pixel_7_API_31 模拟器已启动并覆盖安装 Debug 包。

补充修复：main 的实时文章更新先覆盖 currentArticleDoc 再比较，导致新图片/正文被误判为已显示。现以独立的已渲染快照比较，队列和回复区域独立刷新；保留相同正文的图片 View，长指令队列项限制三行，防止遮挡正文。新增图片更新、文字更新、切换文章和等价快照回归用例。模拟器已重新打开现场文章，确认原先生成的两张图片均显示，没有再次提交生成。583 项 JVM 测试及 Debug 构建通过。

## 长按与语音复查（2026-09-08）

- 图片菜单保留目标图片 key，文字菜单保留段落定位，均携带菜单 itemId。现有请求协议与文字生成逻辑保持一致。
- 修复取消/空识别后清空仍挂载的麦克风及听写气泡引用，导致下一次语音无法更新反馈的问题。页面退出时仍正常释放引用。
- 新增执行实际重置及气泡更新方法的行为回归测试：修复前第二次录入断言失败，修复后通过。
- Android main 下 `./gradlew testDebugUnitTest assembleDebug` 通过，584 项测试、0 失败。
- Pixel_7_API_31 已覆盖安装新版；图片与正文长按菜单均能弹出，连续两次按住并上滑取消，第二次仍显示“在听…”与取消提示。没有为测试提交文章修改。
- 尚未用真实口述完成 ASR→提交指令→后端改图/改文的端到端联调；不能以单元测试和取消录入代替该验证。
